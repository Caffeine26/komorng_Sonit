# components/ui/ — shadcn primitives (LOCAL COPY)

Add primitives via:

```bash
npx shadcn@latest add button input card dialog form
```

These are **local files** owned by this app. Editing them does not affect
any other frontend app — they each have their own copy.
