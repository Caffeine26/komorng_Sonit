# components/layout/

Cross-feature layout shells: `Header`, `Footer`, `Sidebar`, `Shell`. These
are app-wide chrome that wraps any page.

**Keep this folder very small.** If a component is feature-specific, it
belongs in `features/<x>/components/` instead.
